/*
Velo Payments APIs

Testing CountriesAPIService

*/

// Code generated by OpenAPI Generator (https://openapi-generator.tech);

package velopayments

import (
	"context"
	"os"
	"testing"

	"github.com/stretchr/testify/assert"
)

func Test_velopayments_CountriesAPIService(t *testing.T) {

	configuration := NewConfiguration()
	configuration.Servers = ServerConfigurations{
		{
			URL:         os.Getenv("APIURL"),
			Description: "Velo Payments for testing",
		},
	}
	apiClient := NewAPIClient(configuration)

	otoken, err := authWithVelo()
	if err != nil {
		t.Errorf("oauth token not generated")
	}
	token, err := tokenToOAuth2(otoken)
	if err != nil {
		t.Errorf("oauth2 token not generated")
	}

	t.Run("Test CountriesAPIService ListPaymentChannelRulesV1", func(t *testing.T) {
		cases := map[string]struct{ ExpectedStatus int }{
			"valid": {200},
		}

		for k, tc := range cases {
			auth := context.WithValue(context.TODO(), ContextOAuth2, token)
			_, h, err := apiClient.CountriesAPI.ListPaymentChannelRulesV1(auth).Execute()
			if err != nil {
				t.Errorf("TEST %s FAILED with error", k)
			}

			assert.Equal(t, tc.ExpectedStatus, h.StatusCode, "ListPaymentChannelRulesV1: %s - returned 200", k)
		}
	})

	t.Run("Test CountriesAPIService ListSupportedCountriesV2", func(t *testing.T) {
		cases := map[string]struct{ ExpectedStatus int }{
			"valid": {200},
		}

		for k, tc := range cases {
			auth := context.WithValue(context.TODO(), ContextOAuth2, token)
			_, h, err := apiClient.CountriesAPI.ListSupportedCountriesV2(auth).Execute()
			if err != nil {
				t.Errorf("TEST %s FAILED with error", k)
			}

			assert.Equal(t, tc.ExpectedStatus, h.StatusCode, "ListSupportedCountriesV2: %s - returned 200", k)
		}
	})

}
